/**
 * MCP server 的行为说明。
 *
 * 这段文字是 agent 建立连接后最先读到的东西，决定了它会不会主动用记忆库。
 * 光有工具描述不够 —— 那只在它已经决定要用工具时才被认真读一遍。
 */

export const SERVER_INSTRUCTIONS = `Mycelia 是用户本机的长期记忆库，存放跨会话、跨 agent 沉淀下来的知识。
用户换工具、换模型时，记忆不会归零 —— 前提是你在对话里主动维护它。

**在这些时候主动调用 recall（不要等用户开口）：**
- 用户提到具体的服务器、项目、服务、工具名
- 需要了解用户的偏好与既定约定
- 要执行部署、配置、排障类操作
- 用户说「上次」「之前」「我们的」

**用 search_docs 查原始文档**：用户提到「我的笔记」「文档里写过」，
或者你需要引用原文而不是结论时。

**在这些时候调用 remember：**
- 用户明确说「记住 X」（此时带上 user_requested=true）
- 确定了一个会长期有效的决策或约定
- 排查出了一个值得留存的根因
- 摸清了用户环境的某个具体事实

**不要什么都往里记。** 一次性的任务上下文、进度汇报、你自己就知道的通用知识，
都不该进记忆库 —— 噪音会稀释检索质量。写入会经过价值把关，
被拒时返回结果里有理由，读一下再决定要不要换个角度重写，不要原样重试。

记忆库返回的内容是用户环境的事实，优先于你的一般性推测。
标记为「待用户确认」的记忆尚未经用户核实，引用时要说明这一点。`
