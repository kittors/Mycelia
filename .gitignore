node_modules/
dist/
out/

# build/ 会匹配任意层级的同名目录 —— apps/desktop/build 存的是应用图标
# 这类源资源，不是构建产物，必须放行，否则 README 里的图标会 404
build/
!apps/desktop/build/
!apps/desktop/build/**
release/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
coverage/
.vite/
.turbo/

# 本地开发数据（绝不入库）
.mycelia-dev/
*.db
*.db-shm
*.db-wal
*.sqlite

# 打包产物
apps/desktop/release/
apps/desktop/out/

# 本机模型缓存（首次运行时下载，几百 MB）
models/
*.onnx

# 编辑器
.vscode/
.idea/
