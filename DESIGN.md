# Mycelia 桌面端设计规范

## 1. 基调

对齐 Codex Desktop：中性灰阶、几乎不用彩色、克制的信息密度、跟随系统的原生感。

界面本身要退到背景里去。视觉重点只有一个 —— 内容：记忆、文档、图谱。
凡是让用户先注意到「这个界面」而不是「我的知识」的设计，都是错的。

## 2. 颜色

颜色分两层，组件只碰第二层：

1. `styles/tokens.css` 里的 CSS 变量，按 `:root` / `[data-theme='dark']` 切换
2. `@theme inline` 把它们暴露成 Tailwind utility

**层次**用表面亮度表达，三层足够：`canvas`（最底）→ `surface` → `raised`。
不叠阴影堆深度，`overlay` 除外（浮层需要脱离平面）。

**强调色是反色，不是品牌色**：浅色主题下是黑，深色下是白。
一个本地知识工具不需要品牌色来刷存在感。

**功能色**只用于状态：`success` / `warning` / `danger` / `info`。
它们从不用于装饰。

**记忆类型色**（`--kind-*`）刻意压低饱和度。十种类型如果都用高饱和色，
列表会变成调色板。它们的作用是区分，不是吸引注意。

## 3. 字体

系统字体栈 —— 跟随平台才有原生感。基准 13px，行高 1.55。
ID、计数、时间戳用 `.tabular` 等宽数字，避免列表滚动时左右抖动。

## 4. 组件

- 无卡片。分区靠留白和一像素分隔线，不靠边框盒子
- 圆角 6–9px，浮层 10–12px
- 详情用内嵌右侧面板，不用模态框 —— 用户需要一边看详情一边浏览列表
- 图标统一 24×24 网格、1.5 线宽、单色描边，必须有 `aria-label` 或可见文字

## 5. 布局

- 侧边栏 188px，**分组**呈现：概览 / 知识库 / 整理，设置沉在底部
- 顶栏 46px，只放当前位置、进行中的任务、搜索与新建
- 图谱占满画布，控件浮在四角

导航项按动作类型分组。八个平铺项没有层次，用户每次都要从头扫一遍。
低频的配置入口不占主导航的显眼位置。

## 6. 动效

120–240ms，只动 `transform` / `opacity` / `color` / `background-color`。
超过 240ms 的界面反馈会显得迟钝。

- 列表入场逐项错峰，但最多错开 10~14 项，再多就成了等待
- 分段控件用滑块位移表达切换，不是各自变背景色
- 图谱布局分帧迭代，让结构在眼前长出来而不是突然出现
- 尊重 `prefers-reduced-motion`

## 7. 状态

每个空状态都要回答两件事：**为什么是空的**、**我该做什么**。
只写「暂无数据」等于把用户丢在原地。

加载超过一屏用骨架屏占位，避免内容跳动。
错误提示保留原始信息，不吞成「操作失败」。

## 8. 该做 / 不该做

- 知识图谱是产品signature，给它整块画布
- 敏感边界（加密、待确认、未接入）必须一眼可见
- 不用渐变文字、紫蓝 AI 渐变、装饰性色块、超大标题
- 不把破坏性操作藏在语义含糊的图标后面

## 9. 响应

窗口默认 1080×700，最小 860×560。这是常驻查阅的工具，不是要占满屏幕的编辑器。
宽度不足时，右侧详情面板优先让位给主列表。

## 10. 键盘

- `⌘K` 命令面板（同时是搜索入口）
- `⌘N` 新建记忆，`⌘,` 设置，`⌘1..7` 切换视图
- 待确认队列：`J/K` 移动，`A` 收进，`R` 丢弃
- `Esc` 逐层退出：浮层 → 图谱焦点 → 选中态

键盘要能走完主要路径，鼠标是备选而不是唯一。
